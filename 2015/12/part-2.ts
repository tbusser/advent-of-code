export async function solver(input: string): Promise<number> {
	const source = JSON.parse(input);
	console.log(source);

	return -1;
}

export const prompt = 'The sum of all the numbers in the document is';
